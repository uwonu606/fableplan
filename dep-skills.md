# 의존 스킬 매니페스트

fableplan 워크플로가 참조하는 외부 스킬의 단일 출처다. 현재 소비자는
`install.sh` 하나로, 설치 시 아래 표의 스킬 컬럼을 읽어 없는 스킬을
경고한다. 행을 더하면 스크립트 수정 없이 경고 대상이 늘고, 행을 지우면
빠진다. `README.md` 의존 스킬 절과 `SKILL.md` 규칙 절은 아직 손으로
맞춘다 — 이 파일과 어긋나면 이 파일이 기준이다.

종류가 부재 처리를 가른다 — **본질**이면 그 단계째 건너뛰고, **규율**이면
그 규율 없이 단계를 진행한다.

| 스킬 | 단계 | 종류 | 없을 때 |
|---|---|---|---|
| topic-branch | 0 | 본질 | 0단계를 생략하고 지금 트리에서 진행 — 생략 사실과 이유를 최종 보고에 명시 |
| grilling | 1 | 규율 | 부재를 planner 프롬프트에 명시 — 질문 규율 없이 플랜 진행 |
| explore-model | 1 | 본질 | 부재를 planner 프롬프트에 명시 — 모델 검사를 단계째 건너뜀 |
| tdd | 2 | 규율 | 부재를 builder 프롬프트에 명시 — TDD 규율 없이 구현하고 플랜의 검증 절차로 갈음 |
| code-review | 3 | 본질 | 리뷰를 생략 — 테스트/빌드 재실행 검증은 그대로 수행 |
| scoped-commits | 3 | 규율 | 커밋 위임은 하되 통상적인 커밋 메시지로 한 번에 커밋 |

형식 계약(셸 파서가 의존한다): 표의 첫 컬럼이 스킬 이름이고, 스킬 이름은
소문자 알파벳으로 시작한다. `install.sh` 는 앞 공백을 무시한 뒤 `|` 로 시작하는
행의 첫 셀 중 소문자 영문으로 시작하는 것만 스킬 이름으로 읽는다 — 헤더(한글이든
대문자로 시작하는 영문이든)와 구분선(`-`, `:`)은 그래서 자연히 배제되고, 행을
들여써도 마지막 행이 개행 없이 끝나도 읽힌다. 헤더 셀을 소문자 영문으로 바꾸면
파서가 스킬 이름으로 오인하므로 바꾸지 않는다.
